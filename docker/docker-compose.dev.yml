services:
  llm:
    extends:
      file: ./docker/services.compose.yml
      service: llm

  anony-mate-frontend-backend:
    extends:
      file: ./docker/services.compose.yml
      service: anony-mate-frontend-backend
    ports:
      - "8000:8000"